import React from 'react';
import AppRouter from './router/AppRouter';

const StoreApp = () => {
    return (
        <React.Fragment>
            <AppRouter />
        </React.Fragment>
    );
};

export default StoreApp;
